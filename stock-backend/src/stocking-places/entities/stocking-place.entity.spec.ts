import { StockingPlaceEntity } from './stocking-place.entity';

describe('StockingPlaceEntity', () => {
  it('should be defined', () => {
    expect(new StockingPlaceEntity()).toBeDefined();
  });
});
