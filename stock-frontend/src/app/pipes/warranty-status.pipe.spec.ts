import { WarrantyStatusPipe } from './warranty-status.pipe';

describe('WarrantyStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new WarrantyStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
